# The Dependence Terminator project

_"I need your loops, your clause, and your dependences."_

## Getting started

To compile and enable the compiler:
- Source NOELLE, e.g.: `source /path/to/noelle/enable`
- Run `make`
- Run `source enable`

Now the command `dt-opt` should be available.
